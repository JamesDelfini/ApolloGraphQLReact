src/index.js is the root file of server
src/schema.js is the declaration file of the server type definition (typeDefs) of the build query.
src/resolvers.js is the logic file of the server that connects between schmea and datasources(aka API's functions) action.
src/datasources is the logic file of the server that connects directly to a database or third-party API.